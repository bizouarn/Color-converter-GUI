{
  "name": "color-converter-gui",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "npx -y eslint main.js --fix"
  },
  "engines": {
    "node": "16"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  }
}
